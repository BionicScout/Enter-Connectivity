1. Write create contact like so:
let john = contactCreate("FName LName(Optional)",birthday(Date variable),"Johnathan@gmail.com","615-233-1233",contacted(Date Variable),30(Int));

2. Turn contacts into strings and append to that text variable
let dataTXT = textContact(john(Contact Variable));
dataTXT+=textContact(john2);

3. Call the write function, (Username, Password, Contact Text variable)
writeContacts("Barry","password123",dataTXT);

4. Read contacts
var data = await readContacts("Barry","password123");

5. Separate contacts - This shoul return a list of contact variables
var  info = separateContacts(data);